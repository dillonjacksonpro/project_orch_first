// RingPool is a fully header-only template class.
// This translation unit exists solely to satisfy the Makefile prerequisite
// and to verify the header compiles as a standalone unit.
#include "ring/ring_pool.hpp"
